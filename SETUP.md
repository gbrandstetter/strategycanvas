Local dev instructions

- Install dependencies:

```bash
npm install
```

- Run dev server:

```bash
npm run dev
```

This project uses Vite. The UI uses Tailwind via CDN in `index.html` so no build step for Tailwind is required.
